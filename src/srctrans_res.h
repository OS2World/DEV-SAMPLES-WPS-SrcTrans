/* srctrans_res.h - resource constants for srctrans.rc (wrc-safe)
 * Mirrors the defines from srctrans.ph; used by wrc instead of including
 * srctrans.ph (which pulls in pmwp.h and WPS headers wrc doesn't need).
 */
#define IDD_DRAGWIN  100
#define ID_ICON      1001
